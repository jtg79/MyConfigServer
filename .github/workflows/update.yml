name: Update Sub
on:
  schedule:
    - cron: '0 * * * *' # هر یک ساعت یکبار
  workflow_dispatch: # قابلیت اجرای دستی

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4 # این خط اصلاح شد (actions به جای ambassadors)
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install requests

      - name: Run Script
        run: python main.py

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add sub_link.txt
          git commit -m "Update sub: $(date)" || exit 0
          git push
